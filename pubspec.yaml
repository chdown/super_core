name: super_core
description: A new Flutter project.
version: 0.0.1
homepage: https://github.com/chdown/super_core

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ">=2.0.0 <5.0.0"
  decimal: ">=2.0.0 <5.0.0"
  connectivity_plus: ^6.0.5
  dio: ^5.5.0+1
  intl: ^0.19.0
  lottie: ^3.1.2 #lottie动画

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
