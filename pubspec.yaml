name: super_core
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.1
  logger: ^2.1.0
  connectivity_plus: ^5.0.2
  pull_to_refresh: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  super_load:
    git:
      url: https://github.com/chdown/super_load
      ref: 55b127d

flutter:
